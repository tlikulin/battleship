#pragma once

/*
* Contains utility functions such as 
* - reading an int (with validation),
* - exiting the game
*/

// Enter an int from keyboard
int get_int(void);
// Exit the game with a message
void exit_game(int);